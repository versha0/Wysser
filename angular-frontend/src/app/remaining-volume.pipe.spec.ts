import {RemainingVolumePipe} from './remaining-volume.pipe';

describe('RemainingVolumePipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingVolumePipe();
    expect(pipe).toBeTruthy();
  });
});
